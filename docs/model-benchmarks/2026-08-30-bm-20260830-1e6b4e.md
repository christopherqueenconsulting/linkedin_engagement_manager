# Model-tier benchmark — 2026-08-30 (`bm-20260830-1e6b4e`)

- **Scoring mode:** `in-runner-judge`
- **Tiers:** lem-complex
- **Candidates:** mistral-large-3:675b
- **Judge calls spent:** 21 (cap 200)
- **Unmeasured cases:** 1 of 20 — the provider call errored, so those cases produced no output and were never scored; that is not a zero-quality result

Inputs are synthetic prompt templates from `tests/benchmarks/model_tiers/`; no customer content, credentials or production logs appear in this report.

## Scorecard

| Tier | Model | Role | Usage | Cases | Contract | First draft | Judge | Judged | Timeouts | p50 | p90 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| lem-complex | `qwen3.5:397b` | champion | unknown | 10 | 90% (9/10) | 90% (9/10) | 44% | 9 | 0 | 29310 ms | 47050 ms |
| lem-complex | `mistral-large-3:675b` | candidate | Medium (2) | 10 | 90% (9/10) | 50% (5/10) | 40% | 5 | 0 | 5461 ms | 7283 ms |

**Contract** is the share of cases whose failures production would actually consume; **First draft** is the same cases against every check, repairable ones included. Production regenerates against the repairable ones (`lint_repaired`, the #617 comment gate, the post review gate) and skips or holds what still fails, so the gap between the two columns is redraft COST, not shipped defects. The absolute floor is on Contract; First draft is advisory and champion-relative (#910).

**Usage** is the Ollama Cloud usage level (quota class) — `unknown` where ollama.com publishes no level for that model, which is the case for models that are also pullable locally. Supply those with `--usage-levels model=<level>`.

## Per-expectation detail

### `qwen3.5:397b` — lem-complex (champion)

- ⚠️ no output for: `complex-mobile-hook`
- ❌ `complex-mobile-hook` — no output; nothing was measured here
  - empty completion after 2 budget escalation(s) and 1 re-measurement(s)
- 🧠 reasoning headroom — 10 case(s) spent the fixture's whole completion budget thinking and were retried at a larger one: `complex-thought-leadership`, `complex-personal-story`, `complex-industry-news`, `complex-build-receipt`, `complex-carousel-outline`, `complex-newsletter-edition`, `complex-promo-artifact-cta`, `complex-mobile-hook`, `complex-similarity-guard`, `complex-engagement-prompt`
- ⚖️ measurement variance — 1 case(s) came back EMPTY at the full budget and were re-measured at that same budget: `complex-mobile-hook`. One run of one case is a data point, not a verdict — read this model's rates as single-measurement.
- 🧑‍⚖️ `complex-carousel-outline`: does not sound like a practitioner or personal story as required
- 🧑‍⚖️ `complex-industry-news`: The post fails to include a required artefact CTA, uses a contrastive frame, and ends with hashtags instead of a promotional call‑to‑action.
- 🧑‍⚖️ `complex-newsletter-edition`: Missing subscription CTA and incomplete content.
- 🧑‍⚖️ `complex-promo-artifact-cta`: The answer includes a specific time period ('Six months later') that was not supplied in the prompt, violating the contract.
- 🧑‍⚖️ `complex-similarity-guard`: does not include required CTA for an artefact or subscription

### `mistral-large-3:675b` — lem-complex (candidate)

- 🔁 `complex-thought-leadership` — first draft only; production regenerates against this and skips/holds what still fails
  - slop_lint: uses the "it's not X, it's Y" contrastive frame, the construction LinkedIn's 2026 update names first: isn’t the build. It’s
- ❌ `complex-carousel-outline` — production would ship this
  - json_object: JSON wrapped in a markdown fence
- 🔁 `complex-newsletter-edition` — first draft only; production regenerates against this and skips/holds what still fails
  - slop_lint: uses the "it's not X, it's Y" contrastive frame, the construction LinkedIn's 2026 update names first: isn’t perfection; it’s
- 🔁 `complex-promo-artifact-cta` — first draft only; production regenerates against this and skips/holds what still fails
  - slop_lint: uses manufactured "ta-da" transitions that promise a payoff: And the best part?
- 🔁 `complex-similarity-guard` — first draft only; production regenerates against this and skips/holds what still fails
  - slop_lint: uses manufactured "ta-da" transitions that promise a payoff: The result?
- 🧑‍⚖️ `complex-build-receipt`: It invents a 47% statistic not supplied in the prompt.
- 🧑‍⚖️ `complex-engagement-prompt`: The answer is a short prompt, not long‑form LinkedIn content, and it does not ask for an artefact as required.
- 🧑‍⚖️ `complex-industry-news`: Ends with a meeting request and bait closer, violating the contract requirement to ask for an artefact instead.

## Gate verdicts

### `mistral-large-3:675b` on lem-complex — **reject** (vs champion `qwen3.5:397b`)

- ✅ contract floor — 90% vs floor 90%
- ✅ provider answered every case — all cases answered
- ❌ judge floor — 40% vs floor 80%
- ✅ beats champion (contract) — 90% vs champion 90%
- ❌ beats champion (first-draft) — 50% vs champion 90%
- ❌ beats champion (judge) — 40% vs champion 44%
- ⚠️ first-draft yield (advisory) — 50% vs advisory target 90% — 4 case(s) production would have redrafted rather than shipped (advisory: does not change the verdict)
- 💳 usage level — ⚠️ **quota delta unknown** — no usage level for qwen3.5:397b (ollama.com publishes it only for cloud-only models; pass `--usage-levels qwen3.5:397b=<level>`). Treat as an increase until confirmed.

## Swap recommendations

None. No candidate met its tier's thresholds *and* beat the champion on every graded expectation.

---

Generated by `scripts/benchmark_models.py` (issue #721). Deterministic checks are the in-repo linters (`slop_lint`, the comment quality contract, the `LEMComplexityRouter` tier contract); judge scores come from `in-runner-judge`.
